---
description: Sync2Dine backend deploy paths — matches frontend workspace rule
alwaysApply: true
---

# Sync2Dine backend deployment

Same rule as frontend workspace. Canonical copy:

`c:\Users\dolab\Downloads\sync2dine-frontend\.cursor\rules\tradepro-deployment.mdc`

## This repo

| Field | Value |
|-------|-------|
| Path | `c:\Users\dolab\Downloads\sync2dine-backend` |
| Remote | `https://github.com/dolab-jpg/sync2dine-backend.git` |
| Branch | `master` |
| Live API | **https://app.sync2dine.io** |
| Supabase migrations | `supabase/migrations/` |
| Push DB | `npm run supabase:push` |

Deploy secrets: `../sync2dine-frontend/.cursor/local/deploy.env` (gitignored)

Live ship: from frontend run `bash scripts/push-live-local.sh` (API synced from this repo). Git push: `git push origin master`.
