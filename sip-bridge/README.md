# Soho66 SIP bridge (UNSUPPORTED for Cynthia phone AI — legacy; infra folder still named tradepro-sip-bridge)

**Do not use this for AI answering.** Production Cynthia phone AI is **Vapi only** (`VOICE_PROVIDER=vapi`). See [docs/VAPI_SIP.md](../docs/VAPI_SIP.md).

This folder remains only as a historical reference / staff softphone experiments. Setting `VOICE_PROVIDER=local_realtime` or `soho66` will **fail closed** in the API.

Phone AI line purpose in data stores may still be the compat value `aria` (labeled Cynthia AI in the Call Centre UI).
